# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ConnectionTest' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
	
  # Pods for ConnectionTest
pod "GCDWebServer", "~> 3.0"
  target 'ConnectionTestTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ConnectionTestUITests' do
    # Pods for testing
  end

end
